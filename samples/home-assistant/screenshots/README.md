# Dashboard screenshots

Screenshots of the two sample dashboards, captured from a live Home Assistant
instance (dark theme). Embedded from the samples [README](../README.md).

**Core dashboard** (built-in cards):

- `core-section-general-info.png` - the app-status card
- `core-section-current-task.png` - the live-task card (during an upscale run)
- `core-section-local-telemetry.png` - the local gauges + telemetry history graph
- `core-section-remote-telemetry.png` - the remote-pod gauges + history graphs

**Custom dashboard** (HACS: Mushroom + ApexCharts):

- `custom-section-display.png` - the whole custom section (chips header, live-task
  card, band-coloured tiles, ApexCharts graphs)

The dashboards work without these; they are documentation only.
